# 🚀 Dev Server — Локальная отладка фронтенда

Локальный HTTP сервер для разработки фронтенда **без загрузки на ESP32**.

## Быстрый старт

### Python (рекомендуется)

```bash
python dev-server.py
# или с указанием порта
python dev-server.py 3000
```

### Node.js

```bash
node dev-server.js
# или с указанием порта
node dev-server.js 3000
```

## Использование

1. **Запусти dev-server** (порт по умолчанию: 8080)

2. **Открой в браузере:**
   ```
   http://localhost:8080
   ```

3. **Для подключения к ESP32:**
   - Открой консоль браузера (F12)
   - Выполни:
     ```javascript
     AppConfig.ESP32_HOST = '192.168.1.100';  // IP твоего ESP32
     location.reload();
     ```

## Преимущества

✅ **Быстрая разработка** — изменения видны сразу без перезагрузки прошивки  
✅ **Отладка** — DevTools, breakpoints, console.log  
✅ **Hot reload** — просто обнови страницу  
✅ **CORS** — поддержка кросс-доменных запросов  

## Структура

```
Rover/
├── data/              ← Статика (HTML, CSS, JS)
├── dev-server.py      ← Python сервер
├── dev-server.js      ← Node.js сервер
└── DEV-SERVER.md      ← Этот файл
```

## Настройка

Все настройки в `data/config.js`:

```javascript
window.AppConfig = {
  ESP32_HOST: location.hostname,  // Меняй для отладки
  STREAM_PORT: 81,
  CONTROL_API: '/api/control',
  // ...
};
```

## Примеры

### Изменить IP ESP32 в консоли браузера:
```javascript
AppConfig.ESP32_HOST = '192.168.1.50';
location.reload();
```

### Изменить порт стрима:
```javascript
AppConfig.STREAM_PORT = 82;
location.reload();
```

### Изменить expo по умолчанию:
```javascript
AppConfig.CONTROL.expo = 0.5;  // +50%
```
